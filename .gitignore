# Python
__pycache__/
*.py[cod]

# OS
.DS_Store
Thumbs.db

# Local validation aids (base NodeId tables, not distributed)
**/tools/ref/

# Scratch area used by the specification release workflow. The private-repository
# checkout is staged here; the workflow also excludes it by pathspec when staging
# the public commit, and this rule is the second, independent barrier against
# committing a private checkout into this public repository.
node_modules/

# The pinned upstream AAS example corpus (~2400 matched JSON/Turtle pairs).
# Fetched on demand by companion-specs/AAS/jsonld/tools/fetch_corpus.py; the
# hard cases needed for an offline run are vendored in jsonld/fixtures/ instead.
companion-specs/AAS/jsonld/.corpus/

# Local cache of the published IDTA submodel templates, read by
# companion-specs/AAS/jsonld/tools/dpp_inventory.py.
companion-specs/AAS/jsonld/.templates/
