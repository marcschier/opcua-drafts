# OPC UA — Vision — Annex A: Information model (generated)

> Generated by `build_model.py`. Do not edit by hand. Namespace `http://opcfoundation.org/UA/Vision/` (index 1). NodeIds are provisional.

This annex is the authoritative node reference for the specification: it carries the DataType, ValueRank and ModellingRule of every member, the field list of every structure, the value of every enumeration literal, and the full signature of every Method.

## A.1 Type overview

| NodeId | BrowseName | NodeClass | Subtype of |
|---|---|---|---|
| ns=1;i=4001 | HasCalibration | ReferenceType | NonHierarchicalReferences |
| ns=1;i=4002 | MountedOn | ReferenceType | NonHierarchicalReferences |
| ns=1;i=4003 | HasScenePrim | ReferenceType | NonHierarchicalReferences |
| ns=1;i=4005 | ProducedBy | ReferenceType | NonHierarchicalReferences |
| ns=1;i=1005 | OpticsType | ObjectType | BaseObjectType |
| ns=1;i=1006 | IlluminationType | ObjectType | BaseObjectType |
| ns=1;i=1007 | MediaEndpointType | ObjectType | BaseObjectType |
| ns=1;i=1008 | StreamEndpointType | ObjectType | MediaEndpointType |
| ns=1;i=1009 | ClipEndpointType | ObjectType | MediaEndpointType |
| ns=1;i=1010 | VisionMediaManagementType | ObjectType | BaseObjectType |
| ns=1;i=1011 | CoordinateFrameType | ObjectType | BaseObjectType |
| ns=1;i=1012 | VisionCalibrationType | ObjectType | BaseObjectType |
| ns=1;i=1013 | IntrinsicCalibrationType | ObjectType | VisionCalibrationType |
| ns=1;i=1014 | ExtrinsicCalibrationType | ObjectType | VisionCalibrationType |
| ns=1;i=1002 | VisionSensorType | ObjectType | BaseObjectType |
| ns=1;i=1003 | ImageSensorType | ObjectType | VisionSensorType |
| ns=1;i=1004 | Depth3DSensorType | ObjectType | VisionSensorType |
| ns=1;i=1030 | IVisionSimulatedType | ObjectType | BaseInterfaceType |
| ns=1;i=1020 | VisionResultType | ObjectType | BaseObjectType |
| ns=1;i=1021 | InspectionResultType | ObjectType | VisionResultType |
| ns=1;i=1022 | DetectionResultType | ObjectType | VisionResultType |
| ns=1;i=1023 | SegmentationResultType | ObjectType | VisionResultType |
| ns=1;i=1024 | VisionFeedbackType | ObjectType | BaseObjectType |
| ns=1;i=1018 | InferencePipelineType | ObjectType | BaseObjectType |
| ns=1;i=1001 | VisionRootType | ObjectType | BaseObjectType |
| ns=1;i=3001 | VisionRealityKindEnum | DataType | Enumeration |
| ns=1;i=3002 | VisionStreamProtocolEnum | DataType | Enumeration |
| ns=1;i=3003 | VisionClipFormatEnum | DataType | Enumeration |
| ns=1;i=3004 | VisionVideoCodecEnum | DataType | Enumeration |
| ns=1;i=3005 | VisionEndpointStateEnum | DataType | Enumeration |
| ns=1;i=3006 | VisionEndpointAuthenticationEnum | DataType | Enumeration |
| ns=1;i=3009 | VisionResultEvaluationEnum | DataType | Enumeration |
| ns=1;i=3010 | VisionToleranceStatusEnum | DataType | Enumeration |
| ns=1;i=3011 | VisionFeedbackPurposeEnum | DataType | Enumeration |
| ns=1;i=3012 | VisionCalibrationMountEnum | DataType | Enumeration |
| ns=1;i=3013 | VisionFrameRoleEnum | DataType | Enumeration |
| ns=1;i=3014 | VisionDistortionModelEnum | DataType | Enumeration |
| ns=1;i=3015 | VisionSensorModalityEnum | DataType | Enumeration |
| ns=1;i=3050 | VisionPose3DDataType | DataType | Structure |
| ns=1;i=3051 | VisionBoundingBox2DDataType | DataType | Structure |
| ns=1;i=3052 | VisionBoundingBox3DDataType | DataType | Structure |
| ns=1;i=3053 | VisionImageReferenceDataType | DataType | Structure |
| ns=1;i=3054 | VisionIntrinsicsDataType | DataType | Structure |
| ns=1;i=3055 | VisionDetectionDataType | DataType | Structure |
| ns=1;i=3056 | VisionCharacteristicDataType | DataType | Structure |
| ns=1;i=3057 | VisionStreamSessionDataType | DataType | Structure |

## A.2 ReferenceTypes

| NodeId | BrowseName | InverseName | Subtype of | Description |
|---|---|---|---|---|
| ns=1;i=4001 | HasCalibration | IsCalibrationOf | NonHierarchicalReferences | Links a sensor to a calibration currently valid for it. |
| ns=1;i=4002 | MountedOn | HasMounted | NonHierarchicalReferences | Links a sensor to the CoordinateFrame it is rigidly mounted on, for example a robot flange frame for an eye-in-hand camera. |
| ns=1;i=4003 | HasScenePrim | IsScenePrimOf | NonHierarchicalReferences | Links a sensor to the materialized USD prim representing it, when the Server also implements OPC UA - OpenUSD Scene Materialization. The target is expected to be a UsdGeomCameraType instance. Optional: PrimPath remains the portable descriptor. |
| ns=1;i=4005 | ProducedBy | Produces | NonHierarchicalReferences | Links a result to the inference pipeline that produced it. |

## A.3 ObjectTypes

### OpticsType — `ns=1;i=1005`

*Subtype of:* `BaseObjectType`

The lens in front of a sensor. Member names are deliberately aligned with the ILensType of OPC 40100-2 so that a Server implementing both models reports one set of values under two vocabularies.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| FocalLength | Variable | Double | Scalar | Optional | Focal length in millimetres. |
| Aperture | Variable | Double | Scalar | Optional | Current aperture as an f-number. |
| WorkingDistance | Variable | Double | Scalar | Optional | Current object-to-lens distance in metres. |
| MinimumWorkingDistance | Variable | Double | Scalar | Optional | Smallest usable object-to-lens distance in metres. |
| Magnification | Variable | Double | Scalar | Optional | Image size divided by object size. |
| OpticalFormat | Variable | String | Scalar | Optional | Largest sensor diagonal the lens covers, for example 2/3 inch. |
| MountType | Variable | String | Scalar | Optional | Lens mount, for example C, CS, F or M12. |
| LensType | Variable | String | Scalar | Optional | Lens class, for example Entocentric, Telecentric or Fisheye. |

### IlluminationType — `ns=1;i=1006`

*Subtype of:* `BaseObjectType`

A controlled light source associated with a sensor. Member names align with the ILampType and ILightingControllerType of OPC 40100-2.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| LampType | Variable | String | Scalar | Optional | Emitter technology, for example LED, Laser, Xenon or Fluorescent. |
| Wavelength | Variable | Double | Scalar | Optional | Dominant emission wavelength in nanometres. |
| RelativeIntensity | Variable | Double | Scalar | Optional | Current output as a percentage of full capability. |
| LightingMode | Variable | String | Scalar | Optional | Operating mode, for example Continuous, Strobe or Modulated. |
| Quality | Variable | Double | Scalar | Optional | Remaining emitter quality as a percentage; 100 is new. |

### MediaEndpointType (abstract) — `ns=1;i=1007`

*Subtype of:* `BaseObjectType`

Abstract base for a media access point. The endpoint DESCRIBES where media can be obtained; on the default path the media itself never traverses OPC UA. Subtypes add the protocol- or format-specific members.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| EndpointId | Variable | String | Scalar | Mandatory | Identifier of this endpoint, unique within the sensor. |
| EndpointUri | Variable | String | Scalar | Mandatory | Base URI at which the media is served. May be a template that GetStreamEndpoint or GetClip resolves into a session-specific URI. |
| State | Variable | VisionEndpointStateEnum | Scalar | Mandatory | Runtime state of this endpoint. |
| Authentication | Variable | VisionEndpointAuthenticationEnum | Scalar | Mandatory | Credential the media plane requires. Independent of the OPC UA session. |
| SecureTransport | Variable | Boolean | Scalar | Mandatory | True when the media transport itself provides confidentiality, for example RTSPS, SRT with encryption, or HTTPS. Mandatory because clause 12.2 evaluates credential issuance on it: a Server SHALL NOT return a URI embedding a credential unless this is true and the OPC UA SecureChannel is SignAndEncrypt. A client SHALL treat false as meaning the media transport offers no confidentiality, whatever Authentication states. |
| ProfileName | Variable | String | Scalar | Optional | Vendor profile label, for example main or sub. |
| DataChannelSource | Variable | NodeId | Scalar | Optional | NodeId of the Object through which this endpoint's bytes can also be obtained on an OPC UA data channel, per the OPC UA - Data Channels errata proposal. Non-null means the data channel path is offered IN ADDITION to the endpoint's out-of-band path; null or absent means out-of-band only. The target is created by the Server - typically a DataChannelSourceType instance, or any Object implementing IDataChannelSourceType - and is NOT defined by this specification. That proposal is a DRAFT: a conformant Server may leave this null forever. See clause 6.7. |
| DataChannelContentType | Variable | String | Scalar | Optional | IANA media type the data channel carries, for example video/H264 or image/jpeg. Mirrors IDataChannelSourceType.ContentType so a client can learn the payload type from this model alone, without the Data Channels model being present. Meaningful only where DataChannelSource is non-null. |

### StreamEndpointType — `ns=1;i=1008`

*Subtype of:* `MediaEndpointType`

A continuous media stream. A conformant Server SHALL expose at least one instance whose StreamProtocol is Rtsp; every other protocol is optional. This is the default way to obtain live imagery.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| StreamProtocol | Variable | VisionStreamProtocolEnum | Scalar | Mandatory | Protocol of this stream. Rtsp is the mandatory default. |
| ProtocolVersion | Variable | String | Scalar | Mandatory | Version of StreamProtocol served, e.g. '1.0' for RTSP/1.0 (RFC 2326) or '2.0' for RTSP/2.0 (RFC 7826). RTSP 2.0 is not backward compatible with 1.0, so a client that must interoperate without probing reads this. |
| Codec | Variable | VisionVideoCodecEnum | Scalar | Optional | Codec carried by the stream. |
| Width | Variable | UInt32 | Scalar | Optional | Streamed frame width in pixels. |
| Height | Variable | UInt32 | Scalar | Optional | Streamed frame height in pixels. |
| FrameRate | Variable | Double | Scalar | Optional | Streamed frames per second. |
| Bitrate | Variable | UInt32 | Scalar | Optional | Target bitrate in bits per second. |
| MaxSessions | Variable | UInt32 | Scalar | Optional | Maximum concurrent sessions this endpoint will serve. |
| ActiveSessions | Variable | UInt32 | Scalar | Optional | Sessions currently leased. |

### ClipEndpointType — `ns=1;i=1009`

*Subtype of:* `MediaEndpointType`

A still-image access point. A conformant Server SHALL expose at least one instance whose ClipFormat is Jpeg; every other format is optional. In addition to the default URI path, this type MAY publish the encoded image inline as a ByteString so that clients can Read or Subscribe to it - but only within MaxInlineClipSize, which SHALL NOT exceed the Server's ServerCapabilities.MaxByteStringLength. Inline delivery serves single stills; it is not a substitute for a StreamEndpoint.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| ClipFormat | Variable | VisionClipFormatEnum | Scalar | Mandatory | Encoding of clips from this endpoint. Jpeg is the mandatory default. |
| Quality | Variable | UInt32 | Scalar | Optional | Encoder quality 0 to 100 where the format defines one, for example JPEG. |
| Width | Variable | UInt32 | Scalar | Optional | Clip width in pixels. |
| Height | Variable | UInt32 | Scalar | Optional | Clip height in pixels. |
| Retention | Variable | Duration | Scalar | Optional | How long a generated clip remains retrievable at its Uri. |
| InlineDeliveryEnabled | Variable | Boolean | Scalar | Optional | True when LatestClip is published. False, the default, means clients use the URI path exclusively. |
| MaxInlineClipSize | Variable | UInt32 | Scalar | Optional | Largest inline payload this endpoint will publish, in bytes. SHALL NOT exceed Server.ServerCapabilities.MaxByteStringLength, and a Read or Publish response carrying the value is additionally bounded by the Session's MaxResponseMessageSize. See clause 6.4. |
| LatestClip | Variable | ByteString | Scalar | Optional | The most recently produced clip, encoded per ClipFormat. Subscribable: the value changes once per acquisition, which suits one-image-per-part inspection. When the encoded image exceeds MaxInlineClipSize the Server SHALL set the StatusCode to Bad_EncodingLimitsExceeded, and the client SHALL fall back to LatestClipMetadata.Uri, which remains valid. |
| LatestClipMetadata | Variable | VisionImageReferenceDataType | Scalar | Optional | Descriptor of LatestClip, including the out-of-band Uri and Digest. Populated whenever a clip exists, whether or not the bytes are published inline. |

### VisionMediaManagementType — `ns=1;i=1010`

*Subtype of:* `BaseObjectType`

Container and control surface for a sensor's media endpoints. Holds the endpoint folders and the Methods that select, configure and lease them.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| StreamEndpoints | Object |  |  | Mandatory | StreamEndpointType instances offered by this sensor. At least one uses Rtsp. |
| ClipEndpoints | Object |  |  | Mandatory | ClipEndpointType instances offered by this sensor. At least one uses Jpeg. |
| PreferredStreamEndpoint | Variable | NodeId | Scalar | Optional | The StreamEndpoint a client should use unless it has a reason not to. |
| PreferredClipEndpoint | Variable | NodeId | Scalar | Optional | The ClipEndpoint a client should use unless it has a reason not to. |

**Method `GetStreamEndpoint`** (Mandatory) — Lease a stream. Returns a session descriptor whose Uri may embed a time-limited credential; the client opens that Uri with the media protocol. The preferred protocol is advisory - the Server returns what it can serve, which is at minimum RTSP.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Endpoint | NodeId | Scalar | StreamEndpoint to lease. Null selects PreferredStreamEndpoint, or, when that is also null, the first endpoint in StreamEndpoints in BrowseName order that satisfies the request. |
| ProfileName | String | Scalar | Requested profile, or empty for the default. |
| PreferredProtocol | VisionStreamProtocolEnum | Scalar | Advisory protocol preference. |

| Out | DataType | ValueRank | Meaning |
|---|---|---|---|
| Session | VisionStreamSessionDataType | Scalar | The leased session. |
| Endpoint | NodeId | Scalar | The StreamEndpoint that was leased. |

**Method `ReleaseStreamEndpoint`** (Mandatory) — Release a previously leased stream session. A Server SHALL also expire leases automatically at ExpiresAt.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| SessionToken | ByteString | Scalar | Token from GetStreamEndpoint. |

**Method `ConfigureStreamEndpoint`** (Optional) — Change the encoding parameters of a stream endpoint. A Server MAY reject or clamp values it cannot serve; the resulting effective values are readable on the endpoint.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Endpoint | NodeId | Scalar | StreamEndpoint to configure. |
| Codec | VisionVideoCodecEnum | Scalar | Requested codec. |
| Width | UInt32 | Scalar | Requested width in pixels. |
| Height | UInt32 | Scalar | Requested height in pixels. |
| FrameRate | Double | Scalar | Requested frames per second. |
| Bitrate | UInt32 | Scalar | Requested bitrate in bits per second. |

**Method `SelectEndpoint`** (Optional) — Designate the preferred stream and clip endpoints, updating PreferredStreamEndpoint and PreferredClipEndpoint.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| StreamEndpoint | NodeId | Scalar | Preferred stream endpoint, or null to leave unchanged. |
| ClipEndpoint | NodeId | Scalar | Preferred clip endpoint, or null to leave unchanged. |

**Method `GetClip`** (Mandatory) — Obtain a still image: either the frame associated with a given ResultId, or the frame nearest a timestamp. The returned descriptor always carries a Uri. The bytes are returned inline only when RequestInline is true AND the encoded image fits MaxInlineClipSize; otherwise InlineImage is empty and the client uses the Uri.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Endpoint | NodeId | Scalar | ClipEndpoint to use. Null selects PreferredClipEndpoint, or, when that is also null, the first endpoint in ClipEndpoints in BrowseName order that supports Format. |
| ResultId | String | Scalar | Result whose frame is wanted, or empty. |
| Timestamp | UtcTime | Scalar | Frame nearest this time, used when ResultId is empty. |
| Format | VisionClipFormatEnum | Scalar | Requested encoding; Jpeg is always supported. |
| RequestInline | Boolean | Scalar | Ask for the bytes inline in addition to the Uri. |

| Out | DataType | ValueRank | Meaning |
|---|---|---|---|
| Image | VisionImageReferenceDataType | Scalar | Descriptor of the clip. |
| Endpoint | NodeId | Scalar | The ClipEndpoint that served the clip. |
| InlineImage | ByteString | Scalar | Encoded bytes, or empty when not requested or too large. |

### CoordinateFrameType — `ns=1;i=1011`

*Subtype of:* `BaseObjectType`

A named coordinate frame. Frames form a tree through ParentFrame, so a client can compose a chain from a camera frame to a world frame. Roles follow ISO 9787, which standardises WHICH frames exist - note that no standard defines how to CALIBRATE between them, which is why ExtrinsicCalibrationType carries the result explicitly.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| FrameId | Variable | String | Scalar | Mandatory | Identifier referenced by VisionPose3DDataType.FrameId. |
| Role | Variable | VisionFrameRoleEnum | Scalar | Mandatory | Role of this frame. |
| ParentFrame | Variable | NodeId | Scalar | Optional | The frame this one is expressed in; null for a root frame. |
| Transform | Variable | VisionPose3DDataType | Scalar | Optional | Pose of this frame within ParentFrame. |

### VisionCalibrationType (abstract) — `ns=1;i=1012`

*Subtype of:* `BaseObjectType`

Abstract base for a calibration result, carrying the provenance a client needs in order to decide whether to trust it.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| CalibrationId | Variable | String | Scalar | Mandatory | Identifier of this calibration. |
| PerformedAt | Variable | UtcTime | Scalar | Mandatory | When the calibration was computed. |
| Valid | Variable | Boolean | Scalar | Mandatory | False when the Server knows the calibration is stale, for example after a mount change. |
| ResidualError | Variable | Double | Scalar | Optional | Reprojection or fit residual, in the natural unit of the method. |
| Method | Variable | String | Scalar | Optional | Free-text method identifier, for example Zhang, Tsai-Lenz or Daniilidis. |

### IntrinsicCalibrationType — `ns=1;i=1013`

*Subtype of:* `VisionCalibrationType`

Camera intrinsics and lens distortion for a specific image size.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Intrinsics | Variable | VisionIntrinsicsDataType | Scalar | Mandatory | The intrinsic parameters. |

### ExtrinsicCalibrationType — `ns=1;i=1014`

*Subtype of:* `VisionCalibrationType`

The rigid transform between two frames. For a robot cell this is the hand-eye calibration. No ISO, IEC or VDI standard defines the calibration PROCEDURE, so this type carries the resulting transform, the mounting arrangement it applies to, and its residual, which is what a consumer actually needs.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Mount | Variable | VisionCalibrationMountEnum | Scalar | Mandatory | Mounting arrangement the transform applies to. |
| SourceFrame | Variable | NodeId | Scalar | Mandatory | Frame the transform maps from. |
| TargetFrame | Variable | NodeId | Scalar | Mandatory | Frame the transform maps to. |
| Transform | Variable | VisionPose3DDataType | Scalar | Mandatory | Pose of SourceFrame expressed in TargetFrame. |

### VisionSensorType (abstract) — `ns=1;i=1002`

*Subtype of:* `BaseObjectType`

Abstract base for anything that produces imagery or range data. The RealityKind property is what makes the model sim/real symmetric: a physical camera and a simulated one expose the same members, so a client written against this type works against either.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| SensorId | Variable | String | Scalar | Mandatory | Identifier of the sensor, unique within the Server. |
| RealityKind | Variable | VisionRealityKindEnum | Scalar | Mandatory | Whether this sensor is physical, simulated or hybrid. |
| Modality | Variable | VisionSensorModalityEnum | Scalar | Mandatory | What the sensor measures. |
| Manufacturer | Variable | LocalizedText | Scalar | Optional | Device manufacturer. |
| Model | Variable | LocalizedText | Scalar | Optional | Device model designation. |
| SerialNumber | Variable | String | Scalar | Optional | Device serial number. |
| DeviceUri | Variable | String | Scalar | Optional | Transport-level device identifier, for example a GigE Vision or USB3 Vision device id. Lets a client correlate this sensor with the GenICam layer that actually moves the pixels. |
| FrameId | Variable | String | Scalar | Optional | FrameId of this sensor's own camera frame. |
| Media | Object |  |  | Mandatory | Media endpoints and their control surface. |
| Optics | Object |  |  | Optional | Lens in front of this sensor. |
| Illumination | Object |  |  | Optional | Light source associated with this sensor. |
| Calibrations | Object |  |  | Optional | VisionCalibrationType instances for this sensor. |

### ImageSensorType — `ns=1;i=1003`

*Subtype of:* `VisionSensorType`

A two-dimensional imaging sensor. The acquisition parameters use GenICam SFNC 2.8 names and semantics so that a Server bridging a GenICam device can map them one to one, and a client that knows SFNC needs no translation table. This is the layer OPC 40100-2 leaves empty: its VisionImageSensorType adds no members at all.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Width | Variable | UInt32 | Scalar | Mandatory | Image width in pixels (SFNC Width). |
| Height | Variable | UInt32 | Scalar | Mandatory | Image height in pixels (SFNC Height). |
| PixelFormat | Variable | String | Scalar | Mandatory | Pixel format using GenICam PFNC naming, for example Mono8, BayerRG12 or RGB8 (SFNC PixelFormat). |
| ExposureTime | Variable | Double | Scalar | Optional | Exposure time in microseconds (SFNC ExposureTime). |
| Gain | Variable | Double | Scalar | Optional | Analog gain (SFNC Gain). |
| AcquisitionFrameRate | Variable | Double | Scalar | Optional | Frames per second (SFNC AcquisitionFrameRate). |
| TriggerMode | Variable | String | Scalar | Optional | Trigger mode, On or Off (SFNC TriggerMode). |
| TriggerSource | Variable | String | Scalar | Optional | Active trigger source (SFNC TriggerSource). |
| OffsetX | Variable | UInt32 | Scalar | Optional | Region-of-interest x offset (SFNC OffsetX). |
| OffsetY | Variable | UInt32 | Scalar | Optional | Region-of-interest y offset (SFNC OffsetY). |
| BinningHorizontal | Variable | UInt32 | Scalar | Optional | Horizontal binning factor (SFNC BinningHorizontal). |
| BinningVertical | Variable | UInt32 | Scalar | Optional | Vertical binning factor (SFNC BinningVertical). |
| ReverseX | Variable | Boolean | Scalar | Optional | Horizontal flip (SFNC ReverseX). |
| ReverseY | Variable | Boolean | Scalar | Optional | Vertical flip (SFNC ReverseY). |
| Intrinsics | Variable | VisionIntrinsicsDataType | Scalar | Optional | Currently applicable intrinsics, mirroring the active IntrinsicCalibration. |

### Depth3DSensorType — `ns=1;i=1004`

*Subtype of:* `VisionSensorType`

A sensor producing depth or point-cloud data. Point clouds are large and are obtained through a media endpoint, not read as an OPC UA array.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| MinDepth | Variable | Double | Scalar | Optional | Smallest reportable range in metres. |
| MaxDepth | Variable | Double | Scalar | Optional | Largest reportable range in metres. |
| DepthScale | Variable | Double | Scalar | Optional | Metres represented by one unit of the raw depth map. |
| Baseline | Variable | Double | Scalar | Optional | Stereo baseline in metres, where applicable. |
| PointsPerFrame | Variable | UInt32 | Scalar | Optional | Nominal point count per frame. |

### IVisionSimulatedType (abstract) — `ns=1;i=1030`

*Subtype of:* `BaseInterfaceType`

Applied to a sensor whose RealityKind is Simulated or Hybrid. It names the simulator and the scene prim being rendered, which is what makes a synthetic sensor addressable in the same terms as a physical one. When the Server also implements OPC UA - OpenUSD Scene Materialization, PrimPath resolves to a UsdGeomCameraType instance and HasScenePrim points at it directly.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| SimulatorUri | Variable | String | Scalar | Mandatory | Identifier of the simulator or renderer, for example an Isaac Sim instance. |
| StageIdentifier | Variable | String | Scalar | Mandatory | Root layer identifier of the stage being rendered. Uses the same identity contract as OPC UA - OpenUSD Bindings and Scene Materialization. |
| PrimPath | Variable | String | Scalar | Mandatory | Absolute SdfPath of the camera prim this sensor renders from. |
| GroundTruthAvailable | Variable | Boolean | Scalar | Optional | True when the simulator can emit annotator ground truth alongside imagery. |
| RandomizationSeed | Variable | UInt64 | Scalar | Optional | Seed of the active domain-randomization run, so a dataset can be reproduced. |

### VisionResultType (abstract) — `ns=1;i=1020`

*Subtype of:* `BaseObjectType`

Abstract base for a vision result. Unlike OPC 40100-1, whose ResultContent is BaseDataType[] and explicitly not defined, the subtypes of this type define their content. The trust members exist so that a high-risk deployment can log which model version produced a decision and where its explanation lives.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| ResultId | Variable | String | Scalar | Mandatory | Identifier of the result, unique within the Server. |
| CreationTime | Variable | UtcTime | Scalar | Mandatory | When the result was produced. |
| Sensor | Variable | NodeId | Scalar | Optional | Sensor the frame came from. |
| Pipeline | Variable | NodeId | Scalar | Optional | Pipeline that produced it. |
| ModelVersionUsed | Variable | String | Scalar | Optional | Version of the model that produced the result. Required in practice for auditability when the model can be updated in the field. |
| Confidence | Variable | Double | Scalar | Optional | Overall confidence in the range 0.0 to 1.0, where the model reports one. |
| ExplanationUri | Variable | String | Scalar | Optional | Location of an explanation artefact, for example a saliency map. Treated as untrusted input. |
| Frame | Variable | VisionImageReferenceDataType | Scalar | Optional | Reference to the frame this result was computed from. |

### InspectionResultType — `ns=1;i=1021`

*Subtype of:* `VisionResultType`

A machine-vision inspection outcome: a verdict plus the characteristics that produced it. Carrying nominal, actual, tolerance AND uncertainty is what makes the verdict reproducible by a third party, and is why VisionResultEvaluationEnum has a NotDecidable value.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Evaluation | Variable | VisionResultEvaluationEnum | Scalar | Mandatory | Overall verdict. |
| PartId | Variable | String | Scalar | Optional | Identifier of the inspected part. |
| RecipeId | Variable | String | Scalar | Optional | Identifier of the inspection recipe or program applied. |
| Characteristics | Variable | VisionCharacteristicDataType | Array | Mandatory | Measured characteristics. |

### DetectionResultType — `ns=1;i=1022`

*Subtype of:* `VisionResultType`

A robotics-vision perception outcome: zero or more detected instances, each optionally carrying a 6-DoF pose suitable for picking or servoing.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Detections | Variable | VisionDetectionDataType | Array | Mandatory | Detected instances. |
| FrameId | Variable | String | Scalar | Optional | FrameId that detection poses are expressed in. |

### SegmentationResultType — `ns=1;i=1023`

*Subtype of:* `VisionResultType`

A per-pixel labelling outcome. The mask itself is referenced, not inlined.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| LabelClasses | Variable | String | Array | Optional | Class labels present in the mask. |
| Mask | Variable | VisionImageReferenceDataType | Scalar | Mandatory | Reference to the encoded mask image. |

### VisionFeedbackType — `ns=1;i=1024`

*Subtype of:* `BaseObjectType`

The return path into the vision system. It serves three purposes at once: drawing geometry onto the outgoing stream, recording a downstream verdict against a result, and - most importantly - accepting corrected labels that become training data. That last purpose is what turns a deployed inspection system into a learning one. Every Method here is a WRITE and requires explicit authorization.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| OverlayEnabled | Variable | Boolean | Scalar | Optional | True when submitted geometry is rendered onto the outgoing stream. |
| OverlayStyle | Variable | String | Scalar | Optional | Vendor-defined overlay style identifier. |
| OverlayTtl | Variable | Duration | Scalar | Optional | How long submitted overlay geometry remains rendered. |
| MaxInlineFeedbackImageSize | Variable | UInt32 | Scalar | Optional | Largest inline image this surface accepts, in bytes. SHALL NOT exceed Server.ServerCapabilities.MaxByteStringLength, and a Call request carrying the value is additionally bounded by the Session's MaxRequestMessageSize. An oversized payload is rejected with Bad_EncodingLimitsExceeded and the client uses SubmitImageReference instead. See clause 6.4. |

**Method `SubmitDetections`** (Optional) — Push detected geometry back into the vision system. With Purpose set to Overlay the boxes are drawn on the stream; with Purpose set to GroundTruthLabel they are retained as corrected labels for the associated learning job.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Purpose | VisionFeedbackPurposeEnum | Scalar | Why the geometry is being sent. |
| Detections | VisionDetectionDataType | Array | The detections. |
| FrameReference | VisionImageReferenceDataType | Scalar | Frame the detections belong to. |
| InlineImage | ByteString | Scalar | Optional annotated image, accepted only within MaxInlineFeedbackImageSize; otherwise use SubmitImageReference. |

**Method `SubmitInspectionResult`** (Optional) — Record a downstream inspection verdict against a result, for reconciliation with what the vision system originally reported.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| ResultId | String | Scalar | Result being reconciled. |
| Evaluation | VisionResultEvaluationEnum | Scalar | Downstream verdict. |
| Characteristics | VisionCharacteristicDataType | Array | Downstream measurements, where available. |

**Method `SubmitCorrection`** (Optional) — Submit a human-in-the-loop or downstream correction of a previous result. This is the primary source of labelled data for retraining.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| ResultId | String | Scalar | Result being corrected. |
| Purpose | VisionFeedbackPurposeEnum | Scalar | Normally GroundTruthLabel. |
| CorrectedDetections | VisionDetectionDataType | Array | Corrected detections, where the result was a detection. |
| CorrectedCharacteristics | VisionCharacteristicDataType | Array | Corrected characteristics, where the result was an inspection. |
| Reason | LocalizedText | Scalar | Why the correction was made. |
| InlineImage | ByteString | Scalar | Optional corrected or annotated image, accepted only within MaxInlineFeedbackImageSize; otherwise use SubmitImageReference. |

**Method `SubmitImageReference`** (Optional) — The default way to hand an image back: by reference. Used whenever the image exceeds MaxInlineFeedbackImageSize, and preferred in all cases.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Purpose | VisionFeedbackPurposeEnum | Scalar | Why the image is being sent. |
| Image | VisionImageReferenceDataType | Scalar | Descriptor of the image. |
| ResultId | String | Scalar | Associated result, or empty. |

### InferencePipelineType — `ns=1;i=1018`

*Subtype of:* `BaseObjectType`

Binds a sensor to a deployment and publishes the results. The same type serves on-server and off-server inference: when the deployment is remote the Server publishes results it did not compute, and the only observable difference is AiDeployment.InferenceLocation.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| PipelineId | Variable | String | Scalar | Mandatory | Identifier of the pipeline. |
| Sensor | Variable | NodeId | Scalar | Mandatory | Sensor supplying frames. |
| Deployment | Variable | NodeId | Scalar | Mandatory | The deployment executing inference. This is a NodeId, not a reference, and the node it names is NOT defined by this specification - see clause 8.2. Where the Server also implements OPC UA - AI Deployment and Learning it names a DeploymentType instance there, which is what clause 8's provenance argument assumes; a Server that describes its deployment some other way names that node instead. Nothing in this NodeSet references the other model's identifiers, so adopting or ignoring it changes nothing about loading this one. |
| State | Variable | VisionEndpointStateEnum | Scalar | Mandatory | Runtime state of the pipeline. |
| Continuous | Variable | Boolean | Scalar | Optional | True while the pipeline runs on every frame. |
| Results | Object |  |  | Optional | Recent VisionResultType instances produced by this pipeline. |
| Feedback | Object |  |  | Optional | Feedback surface for pushing results back into the vision system. |

**Method `RunInference`** (Optional) — Run inference once, on the current or a specified frame, and return the identifier of the result that was produced.

| In | DataType | ValueRank | Meaning |
|---|---|---|---|
| Timestamp | UtcTime | Scalar | Frame nearest this time, or null for the newest. |

| Out | DataType | ValueRank | Meaning |
|---|---|---|---|
| ResultId | String | Scalar | Identifier of the produced result. |

**Method `StartContinuous`** (Optional) — Begin running inference on every acquired frame.

Takes no arguments and returns none.

**Method `Stop`** (Optional) — Stop continuous inference.

Takes no arguments and returns none.

### VisionRootType — `ns=1;i=1001`

*Subtype of:* `BaseObjectType`

The single well-known entry point for everything in this model. A client starts here, enumerates Sensors, and follows references outward. Mirrors the discovery pattern of OPC UA - OpenUSD Bindings.

| BrowseName | NodeClass | DataType | ValueRank | ModellingRule | Description |
|---|---|---|---|---|---|
| Sensors | Object |  |  | Mandatory | VisionSensorType instances known to this Server. |
| Pipelines | Object |  |  | Optional | InferencePipelineType instances. |
| Frames | Object |  |  | Optional | CoordinateFrameType instances. |

## A.4 DataTypes

### VisionRealityKindEnum — `ns=1;i=3001`

*Subtype of:* `Enumeration`

Whether the sensor observes the physical world, a simulation, or both. This is the sim/real switch: every other member of the model means the same thing regardless of its value.

| Name | Value | Description |
|---|---|---|
| Physical | 0 | A physical device observing the real world. |
| Simulated | 1 | A synthetic sensor rendered by a simulator (e.g. NVIDIA Isaac Sim); ground truth may be available. |
| Hybrid | 2 | A physical device whose output is augmented or replayed through a simulation. |

### VisionStreamProtocolEnum — `ns=1;i=3002`

*Subtype of:* `Enumeration`

Wire protocol of a continuous media stream. Rtsp is the mandatory default: a conformant Server exposes at least one StreamEndpoint using it.

| Name | Value | Description |
|---|---|---|
| Rtsp | 0 | RTSP (RFC 7826/2326). MANDATORY default streaming protocol. |
| Rtsps | 1 | RTSP over TLS. |
| WebRtc | 2 | WebRTC. |
| Srt | 3 | Secure Reliable Transport. |
| Hls | 4 | HTTP Live Streaming. |
| Mjpeg | 5 | Motion JPEG over HTTP. |
| GenDc | 6 | GenICam GenDC container stream. |
| Other | 7 | A protocol identified by the endpoint URI scheme. |
| DataChannel | 8 | The stream is carried on an OPC UA data channel multiplexed onto the SecureChannel the client already has, per the OPC UA - Data Channels errata proposal. OPTIONAL and never a default: see clause 6.7. That proposal is a DRAFT in this repository, not a released OPC UA specification, so a Server is fully conformant without it. |

### VisionClipFormatEnum — `ns=1;i=3003`

*Subtype of:* `Enumeration`

Encoding of a still clip. Jpeg is the mandatory default: a conformant Server exposes at least one ClipEndpoint using it.

| Name | Value | Description |
|---|---|---|
| Jpeg | 0 | JPEG (ISO/IEC 10918). MANDATORY default clip format. |
| Png | 1 | PNG. |
| Tiff | 2 | TIFF. |
| Bmp | 3 | BMP. |
| WebP | 4 | WebP. |
| GenDc | 5 | GenICam GenDC container. |
| Other | 6 | A format identified by the accompanying media type. |

### VisionVideoCodecEnum — `ns=1;i=3004`

*Subtype of:* `Enumeration`

Codec carried by a stream endpoint.

| Name | Value | Description |
|---|---|---|
| H264 | 0 |  |
| H265 | 1 |  |
| Mjpeg | 2 |  |
| Av1 | 3 |  |
| Raw | 4 | Uncompressed frames. |
| Other | 5 |  |

### VisionEndpointStateEnum — `ns=1;i=3005`

*Subtype of:* `Enumeration`

Runtime lifecycle state of a media endpoint or deployment.

| Name | Value | Description |
|---|---|---|
| Inactive | 0 | Declared but not serving. |
| Ready | 1 | Able to serve; no active session. |
| Active | 2 | Serving at least one session. |
| Degraded | 3 | Serving below configured quality. |
| Faulted | 4 | Unable to serve. |

### VisionEndpointAuthenticationEnum — `ns=1;i=3006`

*Subtype of:* `Enumeration`

Authentication a client must present to the media endpoint. This is the media-plane credential, independent of the OPC UA session.

| Name | Value | Description |
|---|---|---|
| None | 0 | No authentication. Appropriate only on an isolated network. |
| Basic | 1 | HTTP/RTSP Basic. |
| Digest | 2 | HTTP/RTSP Digest. |
| Token | 3 | Bearer token, typically the time-limited token returned by GetStreamEndpoint or GetClip. |
| MutualTls | 4 | Client certificate. |

### VisionResultEvaluationEnum — `ns=1;i=3009`

*Subtype of:* `Enumeration`

Overall verdict of a result. Value semantics are aligned with the ResultEvaluationEnum of OPC 40001-101 so that a client already consuming Machinery results needs no new interpretation rules.

| Name | Value | Description |
|---|---|---|
| Undefined | 0 | No verdict available. |
| Ok | 1 | Within tolerance / accepted. |
| NotOk | 2 | Out of tolerance / rejected. |
| NotDecidable | 3 | A verdict was not possible, typically because the measurement uncertainty spans a tolerance limit. |

### VisionToleranceStatusEnum — `ns=1;i=3010`

*Subtype of:* `Enumeration`

Per-characteristic tolerance outcome.

| Name | Value | Description |
|---|---|---|
| InTolerance | 0 |  |
| OutOfTolerance | 1 |  |
| Indeterminate | 2 | Uncertainty spans a tolerance limit. |

### VisionFeedbackPurposeEnum — `ns=1;i=3011`

*Subtype of:* `Enumeration`

Why a client is pushing information back into the vision system.

| Name | Value | Description |
|---|---|---|
| Overlay | 0 | Render the geometry onto the outgoing stream. |
| Reconciliation | 1 | Record a downstream verdict against the result. |
| GroundTruthLabel | 2 | Treat the payload as a corrected label for training. |
| Trigger | 3 | Use the payload as an acquisition or processing trigger. |

### VisionCalibrationMountEnum — `ns=1;i=3012`

*Subtype of:* `Enumeration`

Physical relationship between a camera and the kinematic chain it is calibrated against.

| Name | Value | Description |
|---|---|---|
| EyeInHand | 0 | Camera mounted on the moving flange or tool. |
| EyeToHand | 1 | Camera fixed in the workspace observing the tool. |
| Fixed | 2 | Camera fixed with no associated kinematic chain. |
| Unknown | 3 |  |

### VisionFrameRoleEnum — `ns=1;i=3013`

*Subtype of:* `Enumeration`

Role of a coordinate frame, following the ISO 9787 frame vocabulary. The mechanical interface and the tool are DISTINCT roles: a camera on a robot flange is calibrated to the mechanical interface, while a pick pose has to reach the tool centre point, and a model that cannot tell them apart cannot express the offset between them.

| Name | Value | Description |
|---|---|---|
| World | 0 |  |
| Base | 1 |  |
| MechanicalInterface | 2 | The flange at the end of the last link, to which an end effector is fitted. This is what an eye-in-hand extrinsic calibration resolves to. |
| Tool | 3 | A tool frame, whose origin is a tool centre point. |
| Object | 4 |  |
| Other | 5 |  |
| Camera | 6 | A camera frame. Numbered after the ISO 9787 roles because it is not one of them. |

### VisionDistortionModelEnum — `ns=1;i=3014`

*Subtype of:* `Enumeration`

Lens distortion model the coefficients belong to.

| Name | Value | Description |
|---|---|---|
| None | 0 |  |
| BrownConrady | 1 | Radial and tangential (k1, k2, p1, p2, k3...). |
| KannalaBrandt | 2 | Fisheye / equidistant. |
| RationalPolynomial | 3 |  |
| Other | 4 |  |

### VisionSensorModalityEnum — `ns=1;i=3015`

*Subtype of:* `Enumeration`

What the sensor measures.

| Name | Value | Description |
|---|---|---|
| Area2D | 0 | Two-dimensional area-scan imaging. |
| Line2D | 1 | Line-scan imaging. |
| Depth3D | 2 | Depth or point-cloud sensing. |
| Thermal | 3 |  |
| Multispectral | 4 |  |
| Event | 5 | Event / neuromorphic camera. |
| Other | 6 |  |

### VisionPose3DDataType — `ns=1;i=3050`

*Subtype of:* `Structure`

A rigid-body pose expressed in a named coordinate frame. Position is metres; Orientation is a unit quaternion ordered (x, y, z, w). Covariance is an optional row-major 6x6 matrix over (x, y, z, rx, ry, rz); an empty array means the uncertainty is not reported.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| FrameId | String | Scalar |  | FrameId of the CoordinateFrame this pose is expressed in. |
| Position | Double | Array | 3 | Translation (x, y, z) in metres. |
| Orientation | Double | Array | 4 | Unit quaternion (x, y, z, w). |
| Covariance | Double | Array | 36 | Row-major 6x6 covariance, or empty. |

### VisionBoundingBox2DDataType — `ns=1;i=3051`

*Subtype of:* `Structure`

A box in image space. The origin is the top-left pixel; Rotation is degrees clockwise about the box centre, so 0 denotes an axis-aligned box.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| CenterX | Double | Scalar |  | Box centre x, in pixels. |
| CenterY | Double | Scalar |  | Box centre y, in pixels. |
| Width | Double | Scalar |  | Box width, in pixels. |
| Height | Double | Scalar |  | Box height, in pixels. |
| Rotation | Double | Scalar |  | Rotation about the centre, in degrees. |

### VisionBoundingBox3DDataType — `ns=1;i=3052`

*Subtype of:* `Structure`

An oriented box in three-dimensional space, given by a centre pose and an extent.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| Center | VisionPose3DDataType | Scalar |  | Centre pose of the box. |
| Size | Double | Array | 3 | Extent (x, y, z) in metres. |

### VisionImageReferenceDataType — `ns=1;i=3053`

*Subtype of:* `Structure`

A reference to image bytes that are NOT carried in the OPC UA payload. This is the default way results point at imagery: the Uri is resolved out-of-band, typically through a ClipEndpoint, and verified against Digest.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| Uri | String | Scalar |  | Location of the encoded image. |
| Digest | ByteString | Scalar |  | Cryptographic digest of the referenced bytes. |
| DigestAlgorithm | String | Scalar |  | Digest algorithm; default SHA-256. |
| Format | VisionClipFormatEnum | Scalar |  | Container or encoding of the image. |
| PixelFormat | String | Scalar |  | Pixel format using GenICam PFNC naming, for example Mono8, BayerRG12 or RGB8. |
| Width | UInt32 | Scalar |  | Image width in pixels. |
| Height | UInt32 | Scalar |  | Image height in pixels. |
| SizeBytes | UInt32 | Scalar |  | Encoded size, so a client can decide whether to fetch. |
| Timestamp | UtcTime | Scalar |  | Acquisition time of the frame. |

### VisionIntrinsicsDataType — `ns=1;i=3054`

*Subtype of:* `Structure`

Pinhole intrinsics plus a distortion model, in pixel units, valid for the stated image size. For a simulated sensor these are derived from the USD camera aperture and focal-length attributes.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| Fx | Double | Scalar |  | Focal length in pixels, x. |
| Fy | Double | Scalar |  | Focal length in pixels, y. |
| Cx | Double | Scalar |  | Principal point x, in pixels. |
| Cy | Double | Scalar |  | Principal point y, in pixels. |
| Skew | Double | Scalar |  | Axis skew; 0 for square pixels. |
| DistortionModel | VisionDistortionModelEnum | Scalar |  | Model the coefficients belong to. |
| DistortionCoefficients | Double | Array |  | Coefficients, model-ordered. |
| Width | UInt32 | Scalar |  | Image width these intrinsics are valid for. |
| Height | UInt32 | Scalar |  | Image height these intrinsics are valid for. |

### VisionDetectionDataType — `ns=1;i=3055`

*Subtype of:* `Structure`

One detected instance. This is the robotics-vision payload: a class, a score, and enough geometry to act on - a 2-D box for image-space work, a 3-D box and a 6-DoF pose for picking and visual servoing. Field naming follows the ROS 2 vision_msgs conventions so that a bridge is mechanical.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| DetectionId | String | Scalar |  | Identifier unique within the result. |
| ClassLabel | String | Scalar |  | Human-readable class name. |
| ClassId | UInt32 | Scalar |  | Numeric class identifier within the model label set. |
| Confidence | Double | Scalar |  | Score in the range 0.0 to 1.0. |
| HasBoundingBox2D | Boolean | Scalar |  | True when BoundingBox2D is meaningful. |
| BoundingBox2D | VisionBoundingBox2DDataType | Scalar |  | Image-space box. |
| HasBoundingBox3D | Boolean | Scalar |  | True when BoundingBox3D is meaningful. |
| BoundingBox3D | VisionBoundingBox3DDataType | Scalar |  | Object-space box. |
| HasPose | Boolean | Scalar |  | True when Pose is meaningful. |
| Pose | VisionPose3DDataType | Scalar |  | 6-DoF pose, for example a grasp pose. |
| TrackId | String | Scalar |  | Stable identity across frames, when tracking is done. |

### VisionCharacteristicDataType — `ns=1;i=3056`

*Subtype of:* `Structure`

One measured characteristic of an inspected part. This is the machine-vision payload, and the field set deliberately mirrors QIF (ISO 23952) Results, including measurement uncertainty, so that a QIF document can be produced from it without inventing information.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| CharacteristicId | String | Scalar |  | Identifier of the characteristic measured. |
| Name | String | Scalar |  | Human-readable characteristic name. |
| Nominal | Double | Scalar |  | Design or target value. |
| Actual | Double | Scalar |  | Measured value. |
| Deviation | Double | Scalar |  | Actual minus Nominal. |
| LowerTolerance | Double | Scalar |  | Lower tolerance limit, relative to Nominal. |
| UpperTolerance | Double | Scalar |  | Upper tolerance limit, relative to Nominal. |
| Uncertainty | Double | Scalar |  | Expanded measurement uncertainty (ISO 14253), in the same unit as Actual. 0 means not reported. |
| Unit | EUInformation | Scalar |  | Engineering unit of Nominal, Actual and Deviation. |
| Status | VisionToleranceStatusEnum | Scalar |  | Per-characteristic outcome. |

### VisionStreamSessionDataType — `ns=1;i=3057`

*Subtype of:* `Structure`

A leased media session. The Uri may embed a single-use or time-limited credential, which is why it is returned by a Method rather than published as a browsable Variable.

| Field | DataType | ValueRank | ArrayDimensions | Description |
|---|---|---|---|---|
| SessionToken | ByteString | Scalar |  | Opaque token identifying the lease. |
| Uri | String | Scalar |  | Media URI to open. |
| Protocol | VisionStreamProtocolEnum | Scalar |  | Protocol of the returned URI. |
| ExpiresAt | UtcTime | Scalar |  | Expiry after which the Uri is no longer valid. |
